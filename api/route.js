import {json} from './_lib/onemap.js';
export default async function handler(req,res){try{const start=String(req.query.start||''),end=String(req.query.end||'');if(!/^-?\d+(\.\d+)?,-?\d+(\.\d+)?$/.test(start)||!/^-?\d+(\.\d+)?,-?\d+(\.\d+)?$/.test(end))return json(res,400,{error:'Valid start and end coordinates are required.'});const url=new URL('https://www.onemap.gov.sg/api/public/routingsvc/route');url.search=new URLSearchParams({start,end,routeType:'drive'});const r=await fetch(url);const data=await r.json();if(!r.ok||data.status_message==='NoRoute')throw new Error(data.status_message||'OneMap route failed.');const seconds=Number(data.route_summary?.total_time);const metres=Number(data.route_summary?.total_distance);json(res,200,{minutes:Math.max(1,Math.round(seconds/60)),km:metres/1000,geometry:data.route_geometry||null})}catch(e){json(res,500,{error:e.message})}}
